Add project cover images here matching the paths referenced in lib/data.ts,
e.g. /images/projects/seox-cover.jpg
Until real images are added, the case study hero and cards render an
animated gradient + grid placeholder, so the site works with zero images.
